using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
public class SummaryController : MonoBehaviour
{
    public TextMeshProUGUI title, circle, normal, fire, plants, lightText, dark;

    private void Awake()
    {
        //CreateGameController.Instance.summary = this;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
